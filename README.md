# ToDoList-CRUDApp-Flask

This is a basic CRUD App whose backend is written in flask, while the frontend uses HTML,CSS and Bootstrap 5

to run(First clone and install requirements.txt
```
python main.py
```
